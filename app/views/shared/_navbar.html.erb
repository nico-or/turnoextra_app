<nav class="navbar navbar-expand-lg bg-body-tertiary mb-4">
  <div class="container">
    <%= link_to root_path, class: "navbar-brand" do %>
      <strong><%= t('app.name') %></strong>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= link_to t(".deals"), deals_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to t(".board_games"), boardgames_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to t(".stores"), stores_path, class: "nav-link" %>
        </li>
        <% if Current.user %>
          <li><%= link_to 'Dashboard', dashboard_path, class: "nav-link" %></li>
        <% end %>
      </ul>
      <%= form_with url: search_boardgames_path, method: :get, html: { role: "search", "aria-label": t(".search_form.label") } do |f| %>
        <div class="input-group">
          <%= f.label :q, t(".search_form.label"), class: "visually-hidden" %>
          <%= f.search_field :q, placeholder: t(".search_form.placeholder"), class: "form-control" %>
          <%= f.button name: nil, class: "btn btn-secondary" do %>
            <i class="bi bi-search px-1"></i>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</nav>