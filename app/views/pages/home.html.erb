<hgroup>
  <h1>Turno Extra!</h1>
  <p>Find the best prices for your next boardgame!</p>
</hgroup>

<section>
  <%= render 'boardgames/search_form' %>
</section>

<section>
  <hgroup>
  <h2>Recent Deals</h2>
  <p>Update date: <%= @reference_date.to_fs(:long) %></p>
  </hgroup>
  <div>
    <% @week_deals.each do |boardgame| %>
      <%= render 'boardgames/card', boardgame: %>
    <% end %>
  </div>
</section>