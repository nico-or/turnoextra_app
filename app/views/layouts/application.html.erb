<!DOCTYPE html>
<html lang="<%= I18n.locale %>" data-bs-theme="dark">
  <head>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= display_meta_tags(default_meta_tags) %>
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <%= favicon_link_tag 'favicon.ico' %>
    <%= favicon_link_tag 'favicon-16x16.png', type: "image/png"%>
    <%= favicon_link_tag 'favicon-32x32.png', type: "image/png"%>
    <%= favicon_link_tag 'apple-touch-icon.png', type: "image/png"%>
    <%= favicon_link_tag 'android-chrome-192x192.png', type: "image/png"%>
    <%= favicon_link_tag 'android-chrome-512x512.png', type: "image/png"%>
    <%# Analytics %>
    <%= render 'shared/umami_header' if Rails.env.production? %>
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app %>
    <%= javascript_importmap_tags %>
    <%= yield :head %>
  </head>
  <body class="d-flex flex-column vh-100">
    <%= render 'shared/header' %>
    <%= render 'shared/flash' %>
    <main class="container flex-grow-1">
      <%= yield %>
    </main>
    <%= render 'shared/footer' %>
  </body>
</html>
